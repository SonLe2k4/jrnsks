import style from './style.module.scss';
type Props = {};

const Category = (props: Props) => {
    return <div className={style['wrapper-category']}>this is category</div>;
};

export default Category;
